
//Scroll Reveal

// window.sr = ScrollReveal({duration:1000, scale:0, origin:'bottom',viewport: document.getElementById('fullpage')});
window.sr = new ScrollReveal();

sr.reveal('.wazz-logo', {
	delay:100,
	opacity:  0,
  scale: 0.8,
	easing: 'cubic-bezier(0.6, 0.2, 0.1, 1)',
});

sr.reveal('.coming-fucking-soon', {
	delay:300,
	opacity:  0,
	scale: 0.8,
	easing: 'cubic-bezier(0.6, 0.2, 0.1, 1)',
});

sr.reveal('.subscribe-form', {
	delay:500,
	opacity:  0,
	scale: 0.8,
	easing: 'cubic-bezier(0.6, 0.2, 0.1, 1)',
});

sr.reveal('.wazz-pattern', {
	delay:100,
	opacity:  0,
	scale: 0.8,
	easing: 'cubic-bezier(0.6, 0.2, 0.1, 1)',
});

sr.reveal('.social-sharing', {
	delay:300,
	opacity:  0,
	scale: 0.8,
	easing: 'cubic-bezier(0.6, 0.2, 0.1, 1)',
});
